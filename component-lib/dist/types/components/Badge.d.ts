import React from 'react';
export interface BadgeProps {
    children: React.ReactNode;
}
/** Pill badge — lime (`--accent`) tinted, uppercase, for labels like "WEEK 3 · DAY 2". */
export declare function Badge({ children }: BadgeProps): React.JSX.Element;
